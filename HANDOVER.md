# 引継ぎ情報

> 最終更新: 2026-07-30
> このファイルは**現在の状態**を書く。作業のたびに上書きする（履歴はgitログが持つ）。

---

## 1. リポジトリの状態

- ブランチは `main` のみ。作業ブランチは全て削除済み（`CLAUDE.md` のGit運用ルールどおり）
- 作業は `main` で直接行い、完了後 `git push origin main` する。別ブランチ・PRは作らない

### ⚠️ 最初に `git fetch origin main` すること

**セッションのコンテナは古いスナップショットからクローンされる。** fetchするまで
`origin/main` が実際より前のコミットを指しており、「作業がmainに入っていない」と
誤認する。実際に2026-07-30のセッション開始時、`origin/main` は `6f964a4` を指しており、
fetchで最新に更新された。次のセッションでも同じ誤認が起きた。

```
git fetch origin main
git log --oneline -1 origin/main     # これが本当の main
```

`git remote -v` が `127.0.0.1` を指しているとおり、gitはローカルプロキシ経由。
プロキシの報告が疑わしいときは GitHub API（`mcp__github__list_branches` /
`list_commits`）で直接確認するのが確実。

**古い `main` を `--force` で push してはいけない。** 履歴が消える。
自分のローカルブランチに作業がある場合は `origin/main..<branch>` で
固有コミットを確認し、`origin/main` にリベースしてから main へ push する。

---

## 2. 今回（2026-07-30）やったこと

### 質疑書: Markdownを正本にして二重管理をやめた

同じ32問が `_template/inquiry.md` と `scripts/generate_inquiry.py` の両方にあり、
片方だけ直すとずれる状態だった（§6の「同じ書式を二重に持っている」の一例）。

- `generate_inquiry.py` から質問定義を削除し、**Markdownを読んでExcelに変換する変換器**にした。
  質問の追加・修正はMarkdown側だけで行う
- 読込順は `{案件ID}/inquiry.md` →（無ければ）`_template/inquiry.md`。
  出力先は案件フォルダ。`--source <path.md>` で読込元を直接指定できる
- 記入済みの回答欄は Excel に転記され、未記入欄だけ水色になる。
  `{客先名}` 等のプレースホルダは残したまま水色表示（`{project_id}` と送付日のみ自動で埋まる）
- 変換ルールは `_template/inquiry.md` 冒頭の表に記載。Agent01・CLAUDE.md の記述も揃えた

実ファイルで検証済み（ひな形32問=8節、回答途中のファイル、`\|`・太字・`<br>` を含むセル、
表の無い節、「## 質疑事項」外の表の除外、エラー時 exit 1）。

### マスクツール（`tools/mask/`）: 見えない場所の漏洩を塞いだ

「画面のどこにも出ないのに機密情報が残る」経路を4つ塞いだ。実ファイルで検証済み。

| 経路 | 対処 |
|---|---|
| DOCX/XLSX 文書プロパティ（作成者・最終更新者・タイトル・会社名・管理者） | 保存後のZIP内XMLをテキストノードのみ書き換え |
| DOCX テキストボックス（図形内テキスト） | `w:txbxContent` を走査対象に追加 |
| XLSX シート名 | 置換（重複時は連番） |
| PDF 文書情報（title/author/subject/keywords） | 置換。候補スキャンもメタデータを読むようにした |

さらに:

- **XLSX図形の消失を修正**。openpyxlは図形を読み書きできず保存時に黙って削除するため、
  マスクすると図面注記や吹き出しが消えたファイルが出来ていた。原本の図形パートを
  マスクして復元する方式にした（画像・関係定義・パート種別宣言も復元）
- **XLSX画像の黒塗り選択**をPDFと同様に追加。同じ形式・同じ画素数の黒画像に差し替える。
  EMF/WMF等の塗れない形式は選択不可にして「非対応形式」と表示する
- **設定に検出候補を含めた**。チェックした候補を保存し、読み込んだ値は今回のファイルで
  検出されなくても必ずマスクされる。同じ客先の別ファイルで取りこぼさないため
- マスク後検査の位置表示を具体化（「段落9」→「テキストボックス 段落1」「文書プロパティ(Company)」）

### 旧ブランチから設計知識を回収

12本の作業ブランチのうち8本が `main` に無い内容を持っていたため、有効なものを回収してから削除した。

- 客先確認項目: Agent01（モーター種別・制御電源電圧・安全カテゴリ・盤製作有無）、
  Agent03（メーカー指定・防爆・ELB・規格指定）、Agent05（5項目→22項目）
- Agent08の登録漏れ解消: `agents/08` は存在するのに `CLAUDE.md` が「Agent00〜04」のままで、
  Agent00にも呼び出し手順が無く定義だけ浮いていた
- 選定ルール: 三菱ブレーカー電動機回路選定早見表（0.2〜37kW）、全負荷電流の概算式
- 規格: JIS B 9703（非常停止装置）。安全規格がCE対応案件のみを対象にしていた
- スクリプト: `generate_inquiry.py`（客先質疑書Excel 32問）、`convert_catalog.py`
- 実案件記録 `20260614-001`、`_template/inquiry.md`

### 見つけて直した既存不具合

- `parts_master/index.csv` の `notes` 欄が括弧内カンマを引用しておらず2〜3行目が11列に割れていた。
  CSVとして読むとモータ負荷への適用・スターデルタ始動等のページ参照が失われる状態だった
- `CLAUDE.md` のディレクトリ構成が実体とずれていた（agents 05〜08・logs・電線選定.md 等が未記載）

---

## 3. 意図的に取り込まなかったもの（同じ議論を繰り返さないため）

| 対象 | 理由 |
|---|---|
| カタログ表のCSV 354ファイル | `index.csv` が既に章・表レベルのページ参照を持ち（例: `2.11モータ負荷への適用(p.46-50)`）、「index.csvで該当箇所を特定し該当ページのみ読む」方針で同じ目的を達成できる。加えて80%未満が49表・最悪-318.2%でセルが断片化した表が混ざっており、個別CSVには精度が入らないため信頼度が判断できない |
| `scripts/download_catalog.py` | docstringは「部品カタログフォルダ内かチェック」と書いているが**実際には検証していない**。任意のfile_idをダウンロードできるため最優先ルールに対して危険 |
| `.claude/settings.json`（Drive読み取りの事前承認） | `search_files`/`read_file_content` を事前承認すると、過去の誤読込（Drive全体検索で取引先データを読んだ件）を止める人間のチェックポイントが消える |
| 過去の `agent_log.csv` エントリ | 39行中2行が `result` 欄に判定内容を入れておりスキーマ違反。かつ存在しないパスを参照 |

`convert_catalog.py` は残してある。ただし**案件ごとの単発利用に限る**（カタログ全体を事前変換して溜め込まない）。
出力を使う前に `tables.json` の `accuracy` を必ず確認し、低い表は元のPDFページを読むこと。

---

## 4. マスクツールの残る既知の制約

- **XLSXの図形内画像**: テキストボックスの文字は消えるが、選択しなければ画像は残る。
  画像パネルで選択すれば黒塗りされる
- **塗れない画像形式**: EMF/WMF等のベクタ形式は黒画像を生成できないため選択不可
- **シートの図形から参照されていない画像**: 印刷ヘッダー/フッターに貼った画像などは
  openpyxlが保存時に落とすため出力に残らない。漏洩はしないが**マスクではなく欠落**になる。
  実ファイルで踏んだら対応する
- **数式セル**: キャッシュ済み計算結果がルールに一致した場合、数式ごとマスク後の文字列で
  置き換える（機密情報を数式経由で再現させないことを数式保持より優先している）。
  Excelで一度も計算・保存されていないファイルはキャッシュが無く検出できない

### 動かし方・テスト

```
cd tools/mask
pip install -r requirements.txt
python app.py          # → http://localhost:5000
```

Windowsは `起動.bat`（git pull → 依存インストール → ブラウザ起動まで行う）。
`tools/mask/*マスク設定*.json` は実在の氏名・客先名が入るため `.gitignore` 済み。

---

## 5. 次にやるとよいこと

- **Agent08の運用開始**: 定義・データ構造は揃った（`projects_index.csv` に `scale`/`priority` 列、
  `progress.md` にマイルストーン欄）。実案件を1件通して過不足を見る
- **`20260614-001` の未決事項の消化**: 客先確認8件・内部確定2件が未決のまま。
  客先名・納期が当時未記録で `projects_index.csv` は「未確認」になっている
- **質疑書の実案件適用**: 変換の流れは整備済み（§2）。`20260614-001` にはまだ `inquiry.md` が無い。
  `spec_summary.md` の客先確認8件をひな形に反映して1件通すと、32問との過不足が見える
- **カタログのページ範囲の手動確認**: `選定ルール.md §2` に「要手動確認（総合カタログ.pdf・
  PDFサイズ過大でページ特定不可）」の項目が複数残っている。ユーザーがPDF目次を見て
  `index.csv` の `selection_guide_ref` を埋める必要がある

`CLAUDE.md` の「未決事項」「将来スコープ」も併せて参照。

---

## 6. 引き継ぐ際の注意

- **Google Driveアクセス制限が最優先ルール**。`CLAUDE.md` の該当節を必ず読むこと。
  「部品カタログ」フォルダ以外は検索・一覧・読込を一切しない。読み取り専用
- エージェント定義と出力テンプレートは**同じ書式を二重に持っている**箇所がある
  （Agent05の確認項目 ↔ `_template/panel_spec.md`、Agent01の書式 ↔ `_template/spec_summary.md`）。
  片方だけ直すと食い違う。今回それで実際にずれていた
  - 質疑書32問（`_template/inquiry.md` ↔ `generate_inquiry.py`）は解消済み。
    Markdownを正本にし、スクリプトは変換だけを行う（§2）。同種の重複は
    「片方を正本にして他方は参照・変換に徹する」形で潰すのが方針
