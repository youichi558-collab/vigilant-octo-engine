# 設備設計AIエージェントシステム - プロジェクトルール

## Git運用ルール

- **常に `main` ブランチで直接作業する**
- 別ブランチを作らない・PRを作らない
- 作業完了後は `git push origin main` で直接プッシュする
- **force push(`git push -f` / `--force`)は絶対に行わない**
- 新しいブランチを作成した場合は、必ずmainにマージしてから削除する。force pushでmainを上書きしない

---

## 目的

設備案件の仕様整理・部品選定・見積作成・設計資料作成を支援するAIエージェントシステム。
最終判断は人間が行う。AIは候補提示・確認・記録を担当する。

---

## 🚨 最重要ルール: Google Driveアクセス制限

**このルールは何よりも優先される。違反した場合、ユーザーはコネクタを切断する。**

### アクセス許可

- アクセス可能なのは **「マイドライブ/部品カタログ/」フォルダ以下のみ**
  - 部品カタログ/三菱電機/
  - 部品カタログ/富士電機/
- 上記以外のフォルダ・ファイル(例: 「参考資料/」フォルダ)は、
  - 検索しない
  - 一覧表示しない
  - 読み込まない
  - 内容を参照・引用しない
  - **「部品カタログ」以外を対象とする検索クエリを実行しない**

### 書き込み制限

- Google Drive上のファイルに対する作成・編集・削除・移動は**一切行わない**(読み取り専用)

### 実行前チェック

Google Driveにアクセスするツールを呼び出す前に、必ず以下を自問する:

1. このアクセス先は「部品カタログ」フォルダ(三菱電機/富士電機サブフォルダ含む)の中か?
2. 検索範囲を「部品カタログ」フォルダ内に限定しているか?(Drive全体検索は禁止)

いずれかがNoの場合、そのツール呼び出しは実行しない。

### 過去の経緯

過去にDrive全体検索により「参考資料」フォルダ内の取引先納入仕様書(取引先データ)を
誤って読み込んでしまい、ユーザーから強い指摘を受けた。再発防止のため本ルールを最優先とする。

---

## エージェント構成(現フェーズ)

最初はAgent00〜03のみで運用する(BOM/見積エージェントは後日追加)。

| Agent | 役割 |
|---|---|
| Agent00 設計統括 | 全体管理、各エージェントへの指示、成果物統合 |
| Agent01 仕様解析 | 客先要求 → 電気/機械/安全条件への変換、未決定項目・確認質問の整理 |
| Agent02 標準仕様判断 | 過去実績から標準構成を提案(現状、過去案件データへのアクセス保留中) |
| Agent03 部品選定 | parts_master/index.csv で候補を絞り込み、必要に応じてカタログPDFの該当ページを参照 |

各エージェントの詳細は `/agents/<番号>_<名称>/AGENT.md` を参照。

---

## データソース運用方針

- カタログPDF(三菱電機・富士電機、各20〜60MB)は事前に全文DB化しない
- 代わりに `database/parts_master/index.csv` に軽量インデックスを保持する
  - 品番・カテゴリ・メーカー・PDFファイル名・ページ範囲 等
- 詳細情報が必要な場合のみ、index.csvで該当箇所を特定し、
  「部品カタログ」フォルダ内の対象PDFの該当ページのみを読み込む

---

## 動作ルール(全エージェント共通)

- AIは勝手に確定しない
- 不明な条件は質問する
- 部品選定理由を必ず記録する
- 変更履歴を残す
- 過去案件を優先する
- **指示された範囲以外で勝手に動かない・他フォルダ・他リソースにアクセスしない**

---

## ディレクトリ構成

```
/CLAUDE.md
/agents/
  00_統括/AGENT.md
  01_仕様解析/AGENT.md
  02_標準仕様判断/AGENT.md
  03_部品選定/AGENT.md
/database/
  parts_master/
    index.csv        ← 品番・カテゴリ・メーカー・PDFファイル名・ページ範囲
  rules/
    標準仕様.md
    選定ルール.md    ← 富士SC-NEXT大容量帯対応の保留事項あり
    規格.md
  projects/
    test_agent03_20260613.md   ← Agent03テスト結果(11kW案件)
    test_conveyor_line3_2026/
      spec_summary.md          ← Agent01新フォーマットで作成済み
      parts_selection.md       ← Agent03連携テスト出力済み
```

---

## 現状と進捗(セッション引継ぎ用)

### 完了済み(2026-06-13時点)

- Agent00〜03 AGENT.md: 全て詳細化済み
- Agent01: 新フォーマット(電気条件テーブル+確度列+3区分の未決定項目一覧)確定・反映済み
- Agent03: spec_summary.md未決定項目の引用ルールを追加済み
- index.csv: 三菱/富士の電磁開閉器・ブレーカー4種を登録済み。富士SC-NEXTの容量適用範囲メモ追記済み
- 選定ルール.md: 富士SC-NEXT大容量帯対応の保留事項を追記済み
- Agent01→Agent03連携テスト(ラインNo.3コンベア追加案件): 完了・出力妥当性確認済み

### 未着手タスク(優先度順)

1. **Agent02用の過去案件データの扱い整理**
   - 「参考資料」フォルダは現状アクセス不可のため別途整理が必要
2. **`database/rules/標準仕様.md` の整備**
   - 現状ほぼ空。内容を充実させる
3. **富士電機の大容量帯カタログ追加対応**
   - 「部品カタログ」フォルダに追加されたタイミングで index.csv に新規行を追加する
4. **BOM/見積エージェント(Agent04〜)の追加**
   - 後日

### 既知の制約・仮説

- 富士SC-NEXTの適用上限: 200-240V時15kW/380-440V時30kW(SC65X/SW65Xまで) — 仮説段階
- Agent02は過去案件データへのアクセス保留中のため、現状は参考提案のみ
- index.csv の列構成は現状のまま運用(実用上問題なしと確認済み)
